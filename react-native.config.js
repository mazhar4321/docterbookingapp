module.exports = {
  dependencies: {
    'react-native-vector-icons': {
      platforms: {
        ios: {},
      },
    },
  },
    project: {
      ios: {},
      android: {}, // grouped into "project"
    },
    assets: ["./assets/fonts/"], // stays the same
  };